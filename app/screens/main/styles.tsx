import { StyleSheet } from 'react-native'
import { color } from '../../constants'
export const styles = StyleSheet.create({
  date: {
    fontSize: 35,
    fontWeight: 'bold',
    color: color.white,
    fontStyle: 'italic',
    alignSelf: 'center',
    marginTop: 10
  }
})